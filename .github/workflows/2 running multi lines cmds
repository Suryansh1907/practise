name: Running multi lines in cmd
on: workflow_dispatch
jobs:
  first-job:
    runs-on:ubuntu-latest
    steps:
      - name: Run mutilple commands
        runs:  |
          echo "First"
          echo "Second"
